export default class provincia {
    constructor(ID,name, latitude, longitude, fullname, displayorder) {
        this.ID           = ID;
        this.name         = name;
        this.latitude         = latitude;
        this.longitude = longitude;
        this.fullname    = fullname;
        this.displayorder = displayorder;
    }
}